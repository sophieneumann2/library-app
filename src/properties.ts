export const ROUTES = {
    BASE: "/",
    CHECKOUT: "/checkout",
    HOME: "/home",
    LOGIN: "/login",
    LOGIN_CALLBACK: "/login/callback",
    SEARCH: "/search",
}

export const API_ROUTES = {
    BASE: "http://localhost:8080/api",
}
